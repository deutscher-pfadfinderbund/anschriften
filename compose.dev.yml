services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: anschriften
      POSTGRES_PASSWORD: anschriften
      POSTGRES_DB: anschriften
    ports:
      - "5544:5432"
    volumes:
      - dev-db:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U anschriften"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  dev-db:
